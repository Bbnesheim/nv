{
  "name": "nesheimvatten-shopify-theme",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "setup": "bundle install && npm install",
    "dev": "bash -lc 'export $(grep -v \"^#\" .env | xargs) && shopify theme dev --store=$SHOPIFY_FLAG_STORE'",
    "deploy": "bash -lc 'export $(grep -v \"^#\" .env | xargs); if [ -z \"$THEME_ID\" ]; then shopify theme push --store=$SHOPIFY_FLAG_STORE --json; else shopify theme push --store=$SHOPIFY_FLAG_STORE --theme-id=$THEME_ID --json; fi'",
    "check": "bundle exec theme-check"
  },
  "devDependencies": {}
}
