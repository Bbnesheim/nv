# Shopify CLI expects these. Codespaces can inject them via Secrets.
SHOPIFY_FLAG_STORE=your-store.myshopify.com
SHOPIFY_CLI_AUTH_TOKEN=shpat_xxx
# Optional: set to update an existing theme on deploy
THEME_ID=
